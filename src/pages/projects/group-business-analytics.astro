---
import DeckLayout from '../../layouts/DeckLayout.astro';

const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const chapters = [
  { id: 'demand', href: '#s1', label: '需求诞生' },
  { id: 'modeling', href: '#s3', label: '建模演进' },
  { id: 'reflection', href: '#s9', label: '产品反思' },
  { id: 'running', href: '#s11', label: '真实运行' },
];
const launchedReports = ['会员新增趋势 2.0', '小程序访问趋势', '积分产生 / 消耗趋势', '销售趋势'];
const statusRows = [
  { status: 'warehouse-launched', label: '后续数仓方案已使用', items: ['小程序访问趋势', '积分产生 / 消耗趋势'] },
  { status: 'legacy-launched', label: '已上线 · 仍为旧方案', items: ['会员新增趋势 2.0', '销售趋势'] },
  { status: 'solution-unscheduled', label: '方案完成 · 未排期', items: ['有价券分析'] },
  { status: 'debt-unscheduled', label: '技术债 · 未排期', items: ['会员新增趋势 2.0 数仓改造', '销售趋势数仓改造', '拍照积分订单纳入销售趋势'] },
];
const adoption = [
  ['约 25 个综合商场', '标准能力开放范围'],
  ['季度考核', '集团会员指标进入管理机制'],
  ['至少 20 次下载', '原典型半年分析工作量'],
  ['一次跨商场查询与下载', '集团—商场两级分析后的工作方式'],
];
---

<DeckLayout
  title="集团经营数据分析体系｜QQ星"
  description="从集团考核需求、指标建模到数仓方案演进的数据产品案例。"
>
  <main class="group-analytics-deck" data-group-analytics-deck data-motion-mode="static" data-model-phase-active="all">
    <nav class="chapter-nav" aria-label="集团经营数据分析案例章节">
      {chapters.map((chapter, index) => (
        <a href={chapter.href} data-chapter-link={chapter.id} aria-current={index === 0 ? 'true' : undefined}>
          <span aria-hidden="true">{String(index + 1).padStart(2, '0')}</span>{chapter.label}
        </a>
      ))}
    </nav>

    <section id="s1" data-scene="demand-origin" data-chapter="demand" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">集团经营数据分析体系 · 需求诞生</p>
        <h1>当管理对象从一家商场变成一个集团</h1>
        <p class="lead">集团管理层不需要逐个进入商场页面完成集团分析；他们需要的是集团汇总、商场横向比较和统一考核入口。</p>
        <div class="mall-convergence" aria-label="多个商场数据面板收束为集团视角">
          <div class="mall-stack">
            <article><span>商场 A</span><b>会员 · 订单 · 积分</b></article>
            <article><span>商场 B</span><b>会员 · 订单 · 积分</b></article>
            <article><span>商场 C…</span><b>各自查看 · 各自下载</b></article>
          </div>
          <span class="flow-arrow" aria-hidden="true">→</span>
          <article class="group-lens">
            <span>集团管理视角</span>
            <strong>集团汇总</strong><strong>商场横向比较</strong><strong>统一考核入口</strong>
          </article>
        </div>
      </div>
    </section>

    <section id="s2" data-scene="problem" data-chapter="demand" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">从效率抱怨追到产品问题</p>
        <h2>下载次数多只是症状，真正缺的是集团分析结构</h2>
        <div class="download-story">
          <article><strong>10 个商场</strong><span>× 半年分析</span></article>
          <article><strong>至少 20 次下载</strong><span>单次最多三个月，再由集团人工合并</span></article>
          <article><strong>一次跨商场查询与下载</strong><span>统一口径后仍可继续下钻</span></article>
        </div>
        <div class="analysis-levels" aria-label="集团分析的三层需求">
          <article><b>01 · 集团整体</b><p>先看集团目标与统一指标结果。</p></article>
          <article><b>02 · 商场对比</b><p>使用同一口径横向识别差异。</p></article>
          <article><b>03 · 单商场下钻</b><p>从异常结果定位到具体商场。</p></article>
        </div>
      </div>
    </section>

    <section id="s3" data-scene="member-model" data-chapter="modeling" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">第一个需求 · 会员新增趋势 2.0</p>
        <h2>先定义“新增”，再设计统计记录</h2>
        <p class="statement">集团新增会员 ≠ 各商场新增会员之和</p>
        <div class="definition-grid">
          <article><span>集团新增会员</span><p>集团只统计真正首次开卡的会员。</p></article>
          <article><span>商场新增开卡会员</span><p>会员首次在该商场开卡。</p></article>
          <article><span>商场新增关联会员</span><p>已在其他商场开卡，首次在该商场产生有效会员交互。</p></article>
          <article><span>商场新增会员</span><p>商场新增开卡会员 + 商场新增关联会员。</p></article>
        </div>
        <div class="record-formula" aria-label="会员新增组合统计记录">
          <code>日期 × 商场 × 新增类型 × 来源维度 → 新增数量</code>
          <p>业务口径被转译成可以按集团和商场查询的组合统计记录。</p>
        </div>
      </div>
    </section>

    <section id="s4" data-scene="sales-model" data-chapter="modeling" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">第二个需求 · 销售趋势</p>
        <h2>从订单事实推导经营指标</h2>
        <div class="fact-inputs" aria-label="销售指标的数据事实输入">
          <span>SO</span><span>负数 RSO</span><span>MCUid</span><span>商场 / 门店</span><span>日粒度</span>
        </div>
        <span class="flow-arrow" aria-hidden="true">↓</span>
        <div class="metric-outputs">
          <article><b>销售额</b><p>SO 与负数 RSO 共同计算</p></article>
          <article><b>销售笔数</b><p>按有效订单事实统计</p></article>
          <article><b>销售人数</b><p>按 MCUid 去重</p></article>
          <article><b>连带率</b><p>由对应分子与分母计算</p></article>
          <article><b>老客率</b><p>基于 MCUid 完成新老客判断</p></article>
        </div>
        <div class="record-formula">
          <code>日期 × 商场 / 门店 × 数据来源 × 支付渠道 → 日指标统计记录</code>
          <p><strong>周 / 月比例需要重新聚合分子与分母</strong>，不能简单平均每日百分比。</p>
        </div>
      </div>
    </section>

    <section id="s5" data-scene="first-model" data-chapter="modeling" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">第一代方案</p>
        <h2>把查询结果提前算好，是早期上线的合理取舍</h2>
        <div class="pipeline" aria-label="第一代预聚合统计模型">
          <article><span>01</span><b>业务事件</b><p>触发对应组合统计记录的增量更新。</p></article>
          <article><span>02</span><b>组合统计记录</b><p>预先保存常用维度组合的查询结果。</p></article>
          <article><span>03</span><b>每日兜底重算</b><p>按单日、单商场重新统计，降低同步失败风险。</p></article>
          <article><span>04</span><b>报表查询</b><p>以较低查询成本支持需求快速落地。</p></article>
        </div>
        <p class="note positive">第一代模型不是错误方案；它解决了当时最重要的“先让集团用起来”。</p>
      </div>
    </section>

    <section id="s6" data-scene="model-boundary" data-chapter="modeling" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">使用中暴露边界</p>
        <h2>每增加一个分析维度，也增加一组更新责任</h2>
        <div class="issue-grid">
          <article><b>组合增殖</b><p>商场、来源、渠道和业务维度交叉生成更多统计节点。</p></article>
          <article><b>更新断点</b><p>业务事件可能遗漏、错乱，或需要历史回溯补算。</p></article>
          <article><b>空值组合</b><p>空值也成为维度值，扩大组合和解释成本。</p></article>
          <article><b>每日重算</b><p>兜底机制逐步带来资源消耗和数据风险。</p></article>
          <article><b>持续修数</b><p>统计错误与修复工作频繁出现。</p></article>
          <article><b>扩展责任</b><p>新增平台或维度需要继续扩写更新链路。</p></article>
        </div>
        <p class="note warning">继续扩写组合结果表，会把每个新维度变成新的更新责任。</p>
      </div>
    </section>

    <div class="model-lab-stage" data-model-stage>
      <section id="s7" class="model-lab" data-scene="model-lab" data-chapter="modeling">
        <div class="scene-shell model-lab-shell">
          <div class="model-lab-copy">
            <p class="eyebrow">模型实验台</p>
            <h2>从组合结果，回到可以追溯的业务事实</h2>
            <p>动画表达的是建模决策，不表示旧报表已经自动迁移。</p>
          </div>
          <div class="model-phases" aria-label="数据模型从组合统计表演进为业务对象的前三个阶段">
            <article data-model-phase="problem">
              <span>01 · 组合表暴露问题</span><h3>结果先算好，更新责任随维度增长</h3>
              <div class="combo-nodes"><i>商场×来源</i><i>日期×渠道</i><i>空值组合</i><i>历史重算</i></div>
            </article>
            <article data-model-phase="objects">
              <span>02 · 行为落成对象</span><h3>先记录真实发生了什么</h3>
              <div class="object-list">
                <i data-model-object="daily-active">日活记录</i><i data-model-object="launch">启动日志</i>
                <i data-model-object="page-view">页面访问日志</i><i data-model-object="new-member">新增会员明细</i>
              </div>
            </article>
            <article data-model-phase="query">
              <span>03 · 查询镜头</span><h3>再按管理问题选择分析维度</h3>
              <div class="query-lens">
                {['商场', '平台', '日期', '用户类型', '打开来源', '推广渠道', '页面路径'].map((dimension) => <i data-query-dimension>{dimension}</i>)}
              </div>
            </article>
          </div>
        </div>
      </section>

      <section id="s8" data-scene="object-mapping" data-chapter="modeling" data-model-phase="metrics">
        <div class="scene-shell">
          <p class="eyebrow">04 · 对象如何被投影为指标</p>
          <h2>先保留真实行为，再按问题选择聚合方式</h2>
          <div class="mapping-grid">
            <article data-fact-object="new-member" data-metric="NM"><span>小程序新增会员明细</span><b>NM</b><p>开卡或绑卡事实及其转化与推广上下文</p></article>
            <article data-fact-object="daily-active" data-metric="UV"><span>小程序用户日活记录</span><b>UV</b><p>用户在某平台、商场和日期的日活事实</p></article>
            <article data-fact-object="launch" data-metric="VV"><span>小程序启动日志</span><b>VV</b><p>每次启动及其来源、渠道和会员状态</p></article>
            <article data-fact-object="page-view" data-metric="PV"><span>小程序页面访问日志</span><b>PV</b><p>页面访问事实，并关联日活与启动上下文</p></article>
          </div>
          <p class="boundary-note">按需聚合不等于所有对象合成一个通用宽表，也不等于旧报表已经完成迁移。</p>
        </div>
      </section>
    </div>

    <section id="s9" data-scene="reflection" data-chapter="reflection" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">产品反思</p>
        <h2>页面只是数据产品的最后一层</h2>
        <ol class="reflection-questions">
          <li><span>01</span><b>谁用数据？</b><p>先识别集团管理者与商场经营者。</p></li>
          <li><span>02</span><b>做什么决策？</b><p>明确汇总、比较、考核与下钻动作。</p></li>
          <li><span>03</span><b>指标怎么定义？</b><p>把业务口径写成可复核的计算关系。</p></li>
          <li><span>04</span><b>事实对象是什么？</b><p>区分订单、日活、启动、访问与新增事实。</p></li>
          <li><span>05</span><b>按什么粒度聚合？</b><p>尊重日、商场、门店、用户与事件粒度。</p></li>
        </ol>
        <p class="note positive">预聚合帮助快速上线，明细与埋点对象帮助持续扩展；它们是不同阶段的产品选择。</p>
        <div class="report-scope" aria-label="四类已上线报表范围">
          {launchedReports.map((report) => <span>{report}</span>)}
        </div>
      </div>
    </section>

    <section id="s10" data-scene="technical-debt" data-chapter="reflection" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">技术债 · 完整一章</p>
        <h2>如实区分已上线、旧方案、方案完成与未排期</h2>
        <div class="status-matrix">
          {statusRows.map((row) => (
            <article data-status={row.status}>
              <strong>{row.label}</strong>
              <div>{row.items.map((item) => <span>{item}</span>)}</div>
            </article>
          ))}
        </div>
        <p class="boundary-note"><strong>能力边界：</strong>MCUid 与统一 SO 只是继续关联分析的基础，不表示统一漏斗已经上线。</p>
      </div>
    </section>

    <section id="s11" data-scene="real-running" data-chapter="running" data-motion-scene>
      <div class="scene-shell">
        <p class="eyebrow">真实运行与本人角色</p>
        <h2>从集团考核需求，生长为可以持续演进的数据产品</h2>
        <div class="adoption-grid">
          {adoption.map(([value, label]) => <article><strong>{value}</strong><span>{label}</span></article>)}
        </div>
        <div class="role-boundary">
          <article><span>我主导</span><p>客户沟通、问题抽象、指标与分析维度建模、数据方案研究、集团—商场两级结构、PRD 与原型、协作和交付推动。</p></article>
          <article><span>团队协作</span><p>研发与数据团队完成工程实现；本案例不把数据开发、埋点开发或数仓建设表述为本人独立完成。</p></article>
        </div>
        <a class="project-return" href={`${base}projects/`}>返回项目经历</a>
      </div>
    </section>
  </main>
</DeckLayout>

<script>
  const root = document.querySelector<HTMLElement>('[data-group-analytics-deck]');
  if (root) {
    import('../../scripts/group-business-analytics-motion')
      .then(({ initGroupBusinessAnalyticsMotion }) => initGroupBusinessAnalyticsMotion(root))
      .catch((error) => {
        root.querySelectorAll<HTMLElement>('[data-motion-scene]').forEach((scene) => { scene.dataset.motionState = 'visible'; });
        root.dataset.motionMode = 'fallback';
        root.dataset.motionReady = 'false';
        root.dataset.motionTriggerCount = '0';
        root.dataset.motionPinCount = '0';
        root.dataset.modelPhaseActive = 'all';
        console.error('Group business analytics motion module failed to load', error);
      });
  }
</script>

<style>
  :global(html:has(.group-analytics-deck)) { scroll-behavior: auto; }

  .group-analytics-deck {
    --ga-blue: var(--c-blue); --ga-violet: var(--c-violet); --ga-cyan: var(--c-cyan);
    --ga-green: var(--c-decide); --ga-rose: var(--c-problem); --ga-amber: #f59e0b;
    --ga-panel: var(--glass); --ga-border: var(--glass-border);
    height: auto; min-height: 100vh; overflow: visible; scroll-snap-type: none;
  }

  section[data-scene] { position: relative; display: grid; align-items: center; min-height: 100vh; height: auto; padding: clamp(6rem, 9vw, 8rem) clamp(1.25rem, 5vw, 5rem); scroll-snap-align: none; overflow: clip; }
  section[data-scene]::before { content: ''; position: absolute; inset: 14% auto auto 8%; width: min(32rem, 48vw); height: min(32rem, 48vw); border-radius: 50%; background: radial-gradient(circle, color-mix(in srgb, var(--ga-blue) 12%, transparent), transparent 66%); pointer-events: none; }
  .scene-shell { position: relative; z-index: 1; width: min(1120px, 100%); margin: 0 auto; }
  .eyebrow { color: var(--ga-cyan); font-size: .76rem; font-weight: 800; letter-spacing: .16em; text-transform: uppercase; }
  h1, h2 { max-width: 18ch; margin-top: .75rem; font-size: clamp(2.1rem, 5vw, 4.8rem); line-height: 1.04; letter-spacing: -.045em; }
  h2 { font-size: clamp(1.9rem, 4vw, 3.7rem); }
  .lead { max-width: 54rem; margin-top: 1.4rem; color: var(--text-dim); font-size: clamp(1.05rem, 1.7vw, 1.35rem); line-height: 1.8; }
  .flow-arrow { display: grid; place-items: center; color: var(--ga-cyan); font-size: 2rem; font-weight: 900; }

  .chapter-nav { position: fixed; z-index: 18; right: 1rem; bottom: 1rem; display: flex; gap: .45rem; max-width: calc(100vw - 2rem); padding: .45rem; border: 1px solid var(--ga-border); border-radius: 999px; background: var(--chrome-bg); backdrop-filter: blur(16px); }
  .chapter-nav a { display: inline-flex; gap: .45rem; align-items: center; padding: .55rem .8rem; border-radius: 999px; color: var(--text-dim); font-size: .76rem; text-decoration: none; }
  .chapter-nav a span { font-variant-numeric: tabular-nums; opacity: .65; }
  .chapter-nav a[aria-current='true'] { color: var(--text); background: color-mix(in srgb, var(--ga-blue) 18%, transparent); }
  .chapter-nav a:focus-visible, .project-return:focus-visible { outline: 3px solid var(--ga-cyan); outline-offset: 3px; }

  .mall-convergence, .download-story, .definition-grid, .metric-outputs, .pipeline, .issue-grid, .mapping-grid, .reflection-questions, .status-matrix, .adoption-grid, .role-boundary { display: grid; gap: 1rem; margin-top: clamp(2rem, 5vw, 4rem); }
  .mall-convergence { grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr); align-items: center; }
  .mall-stack { display: grid; gap: .7rem; }
  .mall-stack article, .group-lens, .download-story article, .analysis-levels article, .definition-grid article, .metric-outputs article, .pipeline article, .issue-grid article, .mapping-grid article, .reflection-questions li, .status-matrix article, .adoption-grid article, .role-boundary article, .record-formula, .note { border: 1px solid var(--ga-border); border-radius: 1rem; background: var(--ga-panel); box-shadow: 0 1.2rem 3.5rem rgba(4, 9, 20, .08); }
  .mall-stack article { display: flex; justify-content: space-between; gap: 1rem; padding: 1rem 1.1rem; }
  .mall-stack b { color: var(--text-dim); font-size: .78rem; }
  .group-lens { display: grid; gap: .65rem; padding: 1.4rem; border-color: color-mix(in srgb, var(--ga-blue) 45%, var(--ga-border)); }
  .group-lens span { color: var(--ga-blue); font-size: .76rem; font-weight: 800; letter-spacing: .12em; }

  .download-story { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  .download-story article, .adoption-grid article { display: grid; gap: .65rem; padding: 1.4rem; }
  .download-story strong, .adoption-grid strong { font-size: clamp(1.35rem, 2.3vw, 2.1rem); }
  .download-story span, .adoption-grid span { color: var(--text-dim); line-height: 1.6; }
  .analysis-levels { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 1rem; margin-top: 1rem; }
  .analysis-levels article { padding: 1.2rem; }
  .analysis-levels b { color: var(--ga-cyan); }
  .analysis-levels p, .definition-grid p, .metric-outputs p, .pipeline p, .issue-grid p, .mapping-grid p, .reflection-questions p, .role-boundary p { margin-top: .55rem; color: var(--text-dim); line-height: 1.65; }

  .statement { margin-top: 1.5rem; color: var(--ga-violet); font-size: clamp(1.25rem, 2.4vw, 2rem); font-weight: 900; }
  .definition-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .definition-grid article, .metric-outputs article, .pipeline article, .issue-grid article, .mapping-grid article, .reflection-questions li, .role-boundary article { padding: 1.25rem; }
  .definition-grid span { color: var(--ga-violet); font-weight: 800; }
  .record-formula { margin-top: 1rem; padding: 1.25rem; }
  .record-formula code { color: var(--ga-cyan); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: clamp(.82rem, 1.5vw, 1rem); }
  .record-formula p { margin-top: .7rem; color: var(--text-dim); line-height: 1.65; }

  .fact-inputs { display: flex; flex-wrap: wrap; gap: .65rem; margin-top: 2rem; }
  .fact-inputs span, .report-scope span, .query-lens i, .combo-nodes i, .object-list i, .metric-map i { padding: .65rem .85rem; border: 1px solid var(--ga-border); border-radius: .7rem; background: var(--ga-panel); font-style: normal; }
  .metric-outputs { grid-template-columns: repeat(5, minmax(0, 1fr)); margin-top: 1rem; }
  .metric-outputs b { color: var(--ga-green); }
  .pipeline { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .pipeline span { color: var(--ga-cyan); font: 800 .74rem ui-monospace, monospace; }
  .pipeline b { display: block; margin-top: .65rem; }
  .note { margin-top: 1.25rem; padding: 1rem 1.2rem; line-height: 1.7; }
  .note.positive { border-left: .25rem solid var(--ga-green); }
  .note.warning, .boundary-note { border-left: .25rem solid var(--ga-amber); }
  .issue-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  .issue-grid b { color: var(--ga-rose); }

  .model-lab-stage { position: relative; }
  .model-lab { isolation: isolate; }
  .model-lab-shell { display: grid; grid-template-columns: minmax(16rem, .72fr) minmax(0, 1.28fr); gap: clamp(1.5rem, 5vw, 5rem); align-items: center; }
  .model-lab-copy p:last-child { max-width: 34rem; margin-top: 1rem; color: var(--text-dim); line-height: 1.7; }
  .model-phases { position: relative; display: grid; gap: 1rem; }
  .model-phases > [data-model-phase] { position: relative; display: grid; align-content: center; min-height: 24rem; padding: clamp(1.3rem, 3vw, 2rem); border: 1px solid var(--ga-border); border-radius: 1.4rem; background: var(--ga-panel); }
  .model-phases > [data-model-phase] > span { color: var(--ga-cyan); font-size: .75rem; font-weight: 900; letter-spacing: .1em; }
  .model-phases > [data-model-phase] h3 { margin-top: .7rem; font-size: clamp(1.4rem, 2.8vw, 2.25rem); }
  .combo-nodes, .object-list, .query-lens, .metric-map { display: flex; flex-wrap: wrap; gap: .7rem; margin-top: 1.5rem; }
  .combo-nodes i { border-style: dashed; border-color: color-mix(in srgb, var(--ga-rose) 55%, var(--ga-border)); }
  .object-list i { border-color: color-mix(in srgb, var(--ga-violet) 50%, var(--ga-border)); }
  .query-lens i { border-radius: 999px; border-color: color-mix(in srgb, var(--ga-cyan) 50%, var(--ga-border)); }
  [data-motion-mode='desktop'] .model-lab-stage { height: 100vh; min-height: 44rem; overflow: clip; }
  [data-motion-mode='desktop'] .model-lab-stage > section { position: absolute; inset: 0; width: 100%; min-height: 100%; }
  [data-motion-mode='desktop'] .model-phases { min-height: 28rem; }
  [data-motion-mode='desktop'] .model-phases > [data-model-phase] { position: absolute; inset: 0; width: 100%; }
  [data-motion-mode='desktop'] #s8 { z-index: 3; }

  .mapping-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .mapping-grid article { display: grid; grid-template-columns: 1fr auto; gap: .7rem; }
  .mapping-grid span { color: var(--ga-violet); font-weight: 800; }
  .mapping-grid b { color: var(--ga-green); font-size: 1.3rem; }
  .mapping-grid p { grid-column: 1 / -1; }
  .boundary-note { margin-top: 1.25rem; padding: 1rem 1.2rem; border-radius: .8rem; background: color-mix(in srgb, var(--ga-amber) 9%, var(--ga-panel)); line-height: 1.7; }

  .reflection-questions { grid-template-columns: repeat(5, minmax(0, 1fr)); padding: 0; list-style: none; }
  .reflection-questions span { color: var(--ga-cyan); font: 800 .75rem ui-monospace, monospace; }
  .reflection-questions b { display: block; margin-top: .65rem; }
  .report-scope { display: flex; flex-wrap: wrap; gap: .65rem; margin-top: 1rem; }
  .status-matrix { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .status-matrix article { padding: 1.25rem; }
  .status-matrix article strong { display: block; color: var(--ga-amber); }
  .status-matrix article div { display: flex; flex-wrap: wrap; gap: .6rem; margin-top: .8rem; }
  .status-matrix article span { padding: .5rem .7rem; border-radius: .6rem; background: color-mix(in srgb, var(--ga-amber) 9%, transparent); }
  [data-status='warehouse-launched'] strong { color: var(--ga-green) !important; }
  [data-status='legacy-launched'] strong { color: var(--ga-blue) !important; }
  [data-status='solution-unscheduled'] strong, [data-status='debt-unscheduled'] strong { color: var(--ga-amber) !important; }
  .adoption-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .role-boundary { grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: 1rem; }
  .role-boundary span { color: var(--ga-cyan); font-weight: 900; }
  .project-return { display: inline-flex; margin-top: 1.5rem; padding: .8rem 1rem; border: 1px solid var(--ga-border); border-radius: 999px; color: var(--text); text-decoration: none; }

  [data-motion-ready='true'] [data-motion-scene][data-motion-state='pending'] .scene-shell { opacity: 0; transform: translateY(1.25rem); }
  [data-motion-ready='true'] [data-motion-scene][data-motion-state='visible'] .scene-shell { opacity: 1; transform: none; transition: opacity .5s ease, transform .5s ease; }

  @media (max-width: 1023px) {
    section[data-scene] { min-height: auto; padding: 6.5rem 1.2rem 5rem; overflow: visible; }
    h1, h2 { max-width: 22ch; }
    .chapter-nav { left: 50%; right: auto; width: max-content; transform: translateX(-50%); }
    .chapter-nav a { padding: .55rem .68rem; }
    .chapter-nav a span { display: none; }
    .mall-convergence, .model-lab-shell { grid-template-columns: 1fr; }
    .mall-convergence > .flow-arrow { transform: rotate(90deg); }
    .download-story, .analysis-levels, .pipeline, .metric-outputs, .issue-grid, .mapping-grid, .reflection-questions, .adoption-grid { grid-template-columns: 1fr; }
    .definition-grid, .status-matrix, .role-boundary { grid-template-columns: 1fr; }
    .model-phases { grid-template-columns: 1fr; }
    .model-lab-stage, [data-motion-mode='desktop'] .model-lab-stage { height: auto; min-height: 0; overflow: visible; }
    [data-motion-mode='desktop'] .model-lab-stage > section { position: relative; inset: auto; min-height: auto; }
    .model-phases > [data-model-phase], [data-motion-mode='desktop'] .model-phases > [data-model-phase] { position: relative; inset: auto; min-height: auto; }
  }

  @media (prefers-reduced-motion: reduce) {
    .group-analytics-deck *, .group-analytics-deck *::before, .group-analytics-deck *::after { scroll-behavior: auto !important; transition: none !important; animation: none !important; }
    [data-motion-scene] .scene-shell, [data-model-phase] { opacity: 1 !important; transform: none !important; visibility: visible !important; }
  }
</style>
