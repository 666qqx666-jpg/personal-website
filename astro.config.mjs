import { defineConfig } from 'astro/config';

// 仓库名 personal-website，走 project page，故 base 为 '/personal-website'。
export default defineConfig({
  site: 'https://666qqx666-jpg.github.io',
  base: '/personal-website',
});
