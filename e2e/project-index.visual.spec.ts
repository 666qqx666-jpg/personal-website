import { test, expect } from '@playwright/test';
for (const viewport of [{name:'desktop',width:1280,height:800},{name:'mobile',width:390,height:844}] as const) test(`project index ${viewport.name} visual`, async ({page}) => { await page.setViewportSize(viewport); await page.emulateMedia({reducedMotion:'reduce'}); await page.addInitScript(() => localStorage.setItem('theme','light')); await page.goto('/projects/'); await page.addStyleTag({content:'astro-dev-toolbar{display:none!important}*,*::before,*::after{animation:none!important;transition:none!important}'}); await expect(page).toHaveScreenshot(`project-index-${viewport.name}.png`,{fullPage:true,animations:'disabled'}); });
